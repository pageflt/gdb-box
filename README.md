# gdb-box
```
GDB command for examining memory and displaying in specific byte order:

Usage:
    box/FMT ADDRESS

    - FMT is a repeat count followed by a byte order letter. Byte order letters are l(little-endian) and b(big endian)
    - ADDRESS is an expression for the memory address to examine
```
